

// mileageInfo(car) {
        //     const mileageInfo = car.bookingProps.extraInfo.find(info => info.icon === 'car_repair');
        //     return mileageInfo ? mileageInfo.name : 'No Mileage Found'; // Return the name if found, otherwise 'N/A'
        // },

        // carFuel(car) {
        //     const carFuel = car.bookingProps.extraInfo.find(info => info.icon === 'local_gas_station');
        //     return carFuel ? carFuel.name : 'No Car Fuel Found'; // Return the name if found, otherwise 'N/A'
        // },

        // transmission(car) {
        //     const transmission = car.bookingProps.extraInfo.find(info => info.icon === 'airline_seat_recline_extra');
        //     return transmission ? transmission.name : 'No Car Fuel Found'; // Return the name if found, otherwise 'N/A'
        // },


<!-- // cars() {
    //     let filtered = this.cars;

    //     // if (this.pickupDate && this.dropOffDate) {
    //     //     filtered = filtered.filter((car) => {
    //     //         // Implement your logic to check if car is available during selected dates
    //     //         // Example: check if pickupDate is before car's availabilityEnd date and dropOffDate is after car's availabilityStart date
    //     //         return true;
    //     //     });
    //     // }

    //     filtered = filtered.filter((car) => car.price.salePrice <= this.maxPrice);

    //     // if (this.selectedFuelType !== 'All Fuel Types') {
    //     //     filtered = filtered.filter((car) => car.fuelTypes.includes(this.selectedFuelType));
    //     // }

    //     if (this.selectedCarBrand !== 'All Makers') {
    //         filtered = filtered.filter((car) => {
    //             return car.collections.some(collection => collection.name === this.selectedCarBrand);
    //         });
    //     }

    //     console.log('Filtered cars:', filtered);
    //     return this.paginate(filtered, this.currentPage, this.selectedPageSize);
    // },


    // filteredCars() {

    //     let filtered = this.cars;

    //     // filter by fuel
    //     if (this.selectedFuelType !== 'All Fuel Types') {
    //         filtered = filtered.filter((car) => car.fuelTypes.includes(this.selectedFuelType));
    //     }

    //     // Filter by car maker
    //     if (this.selectedCarBrand !== 'All Makers') {
    //         filtered = filtered.filter((car) => {
    //             return car.brand.some(brand => brand.name.toLowerCase() === this.selectedCarBrand.toLowerCase());

    //         });
    //     }

    //     // Filter by price
    //     filtered = filtered.filter((car) => car.price.salePrice <= this.maxPrice);

    //     return filtered;
    // }, -->

